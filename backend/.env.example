#############################################
# Backend Environment Example (NO REAL SECRETS)
#############################################
# Copy this file to .env and replace placeholder values.
# DO NOT commit your real .env file.

#############################################
# Core Database / App
#############################################
# Full MongoDB connection string.
# Local dev example (requires local Mongo running):
MONGODB_URI=mongodb://127.0.0.1:27017/budgettrackr
# Atlas example (uncomment & edit). Use your own credentials; shown here WITHOUT inline username/password to avoid secret scanners.
# MONGODB_URI=mongodb+srv://<cluster-hostname>/<database>?retryWrites=true&w=majority
# Then supply credentials via MongoDB SRV auth (e.g., in the URI format with URL-encoded user/pass) ONLY in your private .env, never in example.

# Long random string for signing JWTs (at least 32+ chars). REQUIRED in prod.
JWT_SECRET=replace-with-strong-secret

# Node environment: development | production | test
NODE_ENV=development

#############################################
# Network / Server
#############################################
# Public facing bind host & port. Use 0.0.0.0 for Docker/cloud.
PORT=5000
HOST=0.0.0.0

#############################################
# CORS (Origins allowed to call API) - comma separated
#############################################
# If empty: all origins allowed (development convenience)
# Example: CORS_ORIGINS=https://app.example.com,https://admin.example.com
# CORS_ORIGINS=

#############################################
# Email (OTP / notifications) - optional
#############################################
# Gmail credentials (consider using an App Password w/ 2FA account)
# If omitted, mails are logged to console instead of being sent.
# GMAIL_USER=youremail@gmail.com
# GMAIL_PASS=your-gmail-app-password

#############################################
# Advanced / Tuning (optional)
#############################################
# Override default recurring materializer interval (ms). Default ~60000 in code.
# RECUR_INTERVAL_MS=60000

#############################################
# Security Deployment Notes (not variables)
#############################################
# 1. Always set JWT_SECRET in production (never rely on fallback-secret).
# 2. Rotate JWT_SECRET if leaked (forces all users to re-login).
# 3. Use a dedicated least-privilege MongoDB user for production.
# 4. Restrict network access to your MongoDB cluster (IP allowlist / VPC peering).
# 5. Set NODE_ENV=production to enable framework optimizations.

#############################################
# End of file
#############################################

