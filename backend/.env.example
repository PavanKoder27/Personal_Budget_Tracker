#############################################
# Backend Environment Example (no secrets)
#############################################
# Copy to .env and fill in real values for production.

# Mongo connection
MONGODB_URI=mongodb://127.0.0.1:27017/budgettrackr

# JWT signing secret (use a long random string in prod)
JWT_SECRET=replace-with-strong-secret

# Network
PORT=5000
HOST=0.0.0.0

# Environment
NODE_ENV=development
