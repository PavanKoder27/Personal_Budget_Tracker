// Deprecated placeholder kept to avoid stale imports after legacy auth removal.
// Intentionally minimal and does nothing beyond forwarding.
module.exports = function deprecatedAuthBypass(_req, _res, next) { return next(); };
