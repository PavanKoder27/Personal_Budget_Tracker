#############################################
# Frontend Environment Example (Create React App)
#############################################
# Copy to .env (or .env.production / .env.development) and adjust as needed.
# Only variables prefixed with REACT_APP_ are exposed to the browser.

# Base API URL (must include /api at end as the code appends paths directly)
# In development you can usually omit this (fallbacks to localhost:5000),
# but in production set it explicitly to your deployed backend endpoint.
# Example for Render / Railway / Fly.io / AWS etc:
# REACT_APP_API_URL=https://your-backend-domain.example.com/api
REACT_APP_API_URL=

# Example: If deploying frontend as static hosting (Netlify/Vercel) and backend elsewhere,
# set REACT_APP_API_URL to point to backend so axios requests succeed.

# NOTE: Do NOT include secrets here—browser code is public.

#############################################
# End of file
#############################################
